@model IEnumerable<BlogDemo.Models.BlogPost>

<h1>Blog Posts</h1>
<ul>
    @foreach (var post in Model)
    {
        <li>
            <a asp-controller="Blog" asp-action="Details" asp-route-slug="@post.Slug">@post.Title</a>
        </li>
    }
</ul>